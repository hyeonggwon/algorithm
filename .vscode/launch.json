{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug current file from out",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/out/${fileBasenameNoExtension}.out",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": true,
      "MIMode": "lldb",
      "preLaunchTask": "build current file to out"
    }
  ]
}
